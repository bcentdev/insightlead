# Development Environment Variables

# Storage configuration
# Use 'local' for IndexedDB (offline) or 'cloud' for Workers API
VITE_STORAGE_TYPE=local

# API Base URL (only used when VITE_STORAGE_TYPE=cloud)
VITE_API_BASE_URL=http://localhost:8787

# Legacy options (still supported)
# Set to 'true' to use mock data in development
VITE_USE_MOCK_DATA=false

# Set to 'true' to seed IndexedDB with sample data for development
VITE_SEED_INDEXEDDB=false

# GitHub Token (optional)
# Add your GitHub personal access token here to fetch real pull request data
# VITE_GITHUB_TOKEN=your_github_token_here